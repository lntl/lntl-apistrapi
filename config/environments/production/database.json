{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "postgres",
        "host": "${process.env.DATABASE_HOST || 'ec2-54-217-206-65.eu-west-1.compute.amazonaws.com'}",
        "port": "${process.env.DATABASE_PORT || 5432}",
        "database": "${process.env.DATABASE_NAME || 'd75rc1s5b24a3j'}",
        "username": "${process.env.DATABASE_USERNAME || 'cpykouhocxssap'}",
        "password": "${process.env.DATABASE_PASSWORD || '33170e62826617826a956d61c48bfced5fea6cbc2ff912e7f8759109dfe2cb7f'}",
        "ssl": "${process.env.DATABASE_SSL || true}"
      },
      "options": {}
    }
  }
}
